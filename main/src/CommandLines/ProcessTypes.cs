namespace WorkingDirectory.CommandLines
{
    /// <summary>
    /// The types which indicates this app should do.
    /// </summary>
    public enum ProcessTypes
    {
        Unknown = 0,
        SaveMode = 1,
        LaodMode = 2
    }
}
